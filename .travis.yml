sudo: required
services:
  - docker

before_install:
  - docker build -t yvesjudematta/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run yvesjudematta/react-test npm run test -- --coverage

after_success:
  - docker build -t yvesjudematta/multi-client ./client
  - docker build -t yvesjudematta/multi-nginx ./nginx
  - docker build -t yvesjudematta/multi-server ./server
  - docker build -t yvesjudematta/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push yvesjudematta/multi-client
  - docker push yvesjudematta/multi-nginx
  - docker push yvesjudematta/multi-server
  - docker push yvesjudematta/multi-worker
